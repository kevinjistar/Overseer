---
Document: WorkflowDocumentSpecializedSchema
Version: 1.1.1
Date: 2025-12-16

PolicyVersions:
  ValidationPolicy_Global: 1.0.0
  WorkflowPolicy: 1.0.0
  CorrectionMode_Policy: 1.0.0
  System_Agent_Edit_Policy: 1.0.0
---

# WorkflowDocumentSpecializedSchema

## Objet du document

Ce document définit le module spécialisé "Workflow Document" utilisé pour étendre UniversalDocumentSchema via la clé `specialized`.

## Rôle du document dans le système

Niveau: normatif et structurel.  
Ce document sert de référence de structure pour les documents de type workflow (métier ou opérationnel) et leurs composants (states, transitions, rules, integrations, validations).  
Il ne définit pas de logique métier d’exécution, ne déclenche aucune transition, et ne remplace pas le Workflow Engine.

---

# WorkflowDocumentSpecializedSchema

description: "Module spécialisé représentant un workflow métier ou opérationnel.  
Il étend UniversalDocumentSchema via la clé specialized."

specialized:

  states:
    description: "États décrits au niveau du document lui même, distincts du Workflow Engine mais compatibles."
    type: array
    itemStructure:
      id: string
      label: string
      description: string
      metadata:
        type: object
        description: "Métadonnées optionnelles spécifiques au workflow"
      flags:
        type: array
        items: string

  transitions:
    description: "Transitions internes propres à ce workflow documentaire."
    type: array
    itemStructure:
      from: string
      to: string
      conditions:
        type: array
        items:
          type: string
      notes: string

  rules:
    description: "Règles documentaires à respecter dans le workflow."
    type: array
    itemStructure:
      rule: string
      enforcement: string
      severity:
        type: string
        enum: ["info", "warning", "critical"]

  integrations:
    description: "Lien facultatif avec WorkflowEngine (utile pour les agents)."
    type: object
    properties:
      engineWorkflowId: string
      syncStateMapping:
        description: "Correspondance entre les états internes du document et ceux du WorkflowEngine."
        type: array
        items:
          type: object
          properties:
            documentState: string
            engineState: string

  validations:
    description: "Règles de validation supplémentaires propres au document workflow."
    type: array
    itemStructure:
      field: string
      rule: string
      message: string

Fin du document
---

Document Metadata & Changelog

Version: 1.1.0
Date: 2025-12-10
Changelog:
  - 1.1.0 (2025-12-10): Mise à jour pour alignement complet avec WorkflowEngineSchema 1.1.0 et UniversalDocumentSchema 1.1.0
  - 1.0.0 (2025-12-10): Initialisation du module spécialisé Workflow Document
Breaking Changes: Aucun depuis 1.0.0
